RALPH PROGRESS LOG — Phase 1: Agent Loop Architecture Upgrade
================================================================

[init] Source plan: .omc/plans/finance-agent-migration-plan.md (Phase 1, steps 1-5)
[init] Target file: backend/app/service/agent/agent.py (450 lines, read in full)

ENVIRONMENT NOTE (important for future iterations):
- This sandbox has NO Python deps installed (no pip, no openai, no pytest) and the
  __pycache__ shows the real dev env uses cpython-3.11, while this sandbox's
  python3 is 3.14. The real runtime environment is NOT available here.
- Decision: tests must stub sys.modules for 'openai' and
  'app.service.pokeapi.pokeapi_tool' before importing agent.py, so the test suite
  runs with zero installed third-party packages and zero network access. This is
  also the textbook-correct way to unit test "should_continue() with mocked
  memory, no live LLM calls" per the plan's own wording.

DESIGN NOTE on should_continue():
- should_continue(query, memory) is NOT a hardcoded heuristic - it still calls the
  LLM internally (it's the refactored reflection()). "No live LLM call" in the
  unit tests means the test suite mocks the LLM-calling boundary (_llm_json),
  not that should_continue() itself avoids calling the LLM in production. This
  preserves Principle 2 (continue/stop must be LLM-driven), confirmed against
  the plan's RALPLAN-DR Principle 2 and the deep-interview spec's success
  criteria (no hardcoded branching deciding the next action).

PLANNED STORY ORDER: US-1 -> US-2 -> US-3 -> US-4 -> US-5 (US-3 could run parallel
to US-1/2 since process_actions() error handling is independent, but doing it
in sequence keeps agent.py edits coherent in one pass).

[done] All 5 stories implemented in a single coherent edit pass to agent.py
(US-3 error propagation, US-1 should_continue() extraction with structured
{sufficient, rationale, actions} contract, US-2 bounded while-loop with
explicit cap-stop vs llm-stop vs no-actions-stop distinction, US-4 rationale
streamed into existing {role:'agent', content} SSE shape - no schema change).

Files changed:
- backend/app/service/agent/agent.py (process_actions error propagation;
  reflection() replaced by should_continue() with structured contract;
  final_answer()'s single reflection call replaced by bounded while-loop)
- backend/tests/test_agent_loop.py (new, 11 unit tests)

Test run: `cd backend && python3 -m unittest tests.test_agent_loop -v`
Result: Ran 11 tests in 0.005s — OK (all passed). Verified no remaining
references to the old reflection()/reflect_plan names via grep.

Key implementation decisions for future iterations / Phase 2 context:
- should_continue() still calls the LLM in production (it IS the refactored
  reflection() function) - "no live LLM call" in tests means the test suite
  mocks _llm_json, not that the function itself is heuristic-only. This
  preserves genuine LLM-driven continue/stop decisions (Principle 2).
- MAX_REFLECTION_ITERATIONS = 5 is a safety cap, not a target iteration count;
  hitting it logs "触达安全上限" distinctly from "LLM 判定信息已足够".
- Tool errors now land in memory as {"提问", "结果": "[工具调用失败] ...", "错误": True}
  so should_continue()'s LLM call can see and react to failures.
- Test suite stubs `openai` and `app.service.pokeapi.pokeapi_tool` via
  sys.modules before importing agent.py, since this sandbox has zero Python
  deps installed and the real dev env (cpython-3.11, per __pycache__) isn't
  present here. Phase 2/5 work that needs a real running backend will need to
  address this environment gap (confirm with user whether a dev venv exists
  outside this sandbox, or whether deps need installing here).

[done] Architect reviewer verification (Step 7): APPROVED. Verified should_continue()
is genuinely LLM-driven with no hardcoded heuristic in the judgment itself,
the while/else cap-stop vs llm-stop distinction is correct including the
last-iteration edge case, error propagation reaches should_continue(), no
leftover reflection()/reflect_plan references, and all 11 tests are
behavioral (not tautological). Fresh test re-run by architect: 11/11 pass.

[done] Deslop pass (Step 7.5): removed the unused `stop_reason` local variable
in final_answer() (assigned in all 4 branches, never read — the print
statements already satisfy the "distinct cap-stop vs llm-stop log" PRD
requirement on their own). No other slop found: _extract_json_object vs
_extract_json_list are legitimately separate (different bracket shapes,
different callers), no unused imports in the test file, no TODO/FIXME
leftovers.

[done] Regression re-verification (Step 7.6): re-ran `python3 -m unittest
tests.test_agent_loop -v` after the deslop edit — 11/11 still pass, syntax
re-checked with py_compile. No regression from the cleanup.

PHASE 1 COMPLETE. All 5 PRD stories pass, architect-approved, deslopped,
regression-verified. Per the project's phased-conversation-handoff plan
(agreed with the user before launch), Phase 2 should start in a FRESH
conversation, handed: this progress.txt, prd.json (all passes:true - can be
archived/reset for Phase 2's own PRD), the plan file
.omc/plans/finance-agent-migration-plan.md, and the environment note above
(no Python deps installed in this sandbox; confirm with user whether a real
dev venv exists elsewhere for Phase 2/5 work that needs the actual backend
running).

================================================================
RALPH PROGRESS LOG — Phase 4: Frontend Re-skin (2026-07-01)
================================================================

[init] Source plan: .omc/plans/finance-agent-migration-plan.md (Phase 4, steps 12-13)
[init] Active PRD: .omc/state/sessions/dfc594af-64d6-4919-8f15-0aff2ed58294/prd.json
       (root prd.json/this progress.txt are the legacy Phase 1 artifacts kept for
       history; Phase 4 tracked stories live in the session-scoped PRD per current
       ralph convention)
[init] Confirmed via commit aed00d8 + README status lines that Phases 1-3 are
       already complete and committed; only frontend/src/i18n.tsx still had
       Pokemon-domain copy (grep confirmed no other Pokemon/Smogon/PokeAPI
       references anywhere else in frontend/src).

DESIGN NOTE on hotQuestions content:
- Picked 5 example questions per plan step 12 spanning the actual query types
  in .omc/eval/finance_eval_set.md (filing lookup, news, educational) plus the
  finance_query tool (price/fundamentals) and web_search (general market
  moves), against the actually-indexed tickers (AAPL/MSFT/GOOGL) rather than
  inventing new ones. This keeps the demo copy honest against what the backend
  can actually answer.

[done] US-1: Rewrote i18n.tsx zh+en blocks (tagline, bannerTitle,
bannerSubtitle, hotQuestions x5) to finance domain. Left attachment/
navNewChat/repoTitle/etc. untouched (domain-agnostic UI chrome, out of
Phase 4's copy-swap scope).

[done] US-2: grep -rniE 'pokemon|pokémon|宝可梦|smogon|pokeapi|garchomp|dragapult'
frontend/src → zero matches. git diff --stat confirms only i18n.tsx changed
under frontend/ (no component/layout files touched).

[done] US-3: Updated README.md — migration-status Phase 4 line ⏳→✅, Features
heading "Phases 1-3"→"Phases 1-4", removed the "Frontend still
Pokemon-themed" Known Limitations bullet, struck through Roadmap item 3
matching the ~~Phase 2~~/~~Phase 3~~ pattern. (Caught and fixed a lost blank
line before the "## Roadmap" heading introduced by one of these edits.)

[done] US-4: `cd frontend && npx tsc -b` → 3 pre-existing TS6133 (unused var)
errors in hot-questions/index.tsx, sender/index.tsx, chat/index.tsx — verified
via `git stash` that these are identical before and after the i18n.tsx edit,
i.e. pre-existing baseline issues unrelated to this change, not a regression
introduced by Phase 4.

NOTE (untracked artifact, not touched): backend/app/service/core/9b5ad71b2ce...
is an untracked stray file (looks like a raw BPE/tokenizer vocab dump, ASCII,
100k+ lines) sitting in the repo working tree. Unrelated to Phase 4 scope —
flagged for the user/next session to decide whether to delete or .gitignore.

PHASE 4 COMPLETE. All 4 PRD stories pass. Diff limited to frontend/src/i18n.tsx
(content-only) and README.md (status markers). No backend changes. Ready for
reviewer verification (Step 7).

[done] Architect reviewer verification (Step 7): APPROVED. Verified US-1
through US-4 with fresh evidence (grep, tsc diff/stash comparison, rendered
README section read). Confirmed hotQuestions cover 4 distinct query types
(filing, price/fundamentals, news, educational) against exactly the indexed
tickers (AAPL/MSFT/GOOGL), domain-agnostic i18n keys untouched, and the 3
tsc errors are provably pre-existing (byte-identical with i18n.tsx stashed).

[done] Deslop pass (Step 7.5): reviewed frontend/src/i18n.tsx and README.md
against the slop checklist (duplication, dead code, needless abstraction,
boundary violations, missing tests, UI/design defaults). This is a pure
copy/status change with no logic - nothing to clean. No edits made.

[done] Regression re-verification (Step 7.6): re-ran grep (zero Pokemon
matches) and `npx tsc -b` (same 3 pre-existing errors, unrelated files) -
no regression from the (no-op) deslop pass.

PHASE 4 COMPLETE. All 4 PRD stories pass, architect-approved, deslop
reviewed (no changes needed), regression-verified. Migration (Phases 1-4)
is now fully complete per the finance-agent-migration-plan.md. Only
remaining plan item is Phase 5 (end-to-end verification / demo trace
inspection), which is out of scope for this run (user asked for Phase 4
only).

NOTE for future session: an untracked stray file
backend/app/service/core/9b5ad71b2ce5302211f9c61530b329a4922fc6a4 (looks
like a raw tokenizer vocab dump) sits in the working tree, unrelated to
Phase 4 - flagged for the user to decide whether to delete or .gitignore,
not touched by this run.

================================================================
RALPH PROGRESS LOG — Phase 5: End-to-End Verification (2026-07-01)
================================================================

[init] Source plan: .omc/plans/finance-agent-migration-plan.md (Phase 5, steps 14-17)
[init] Active PRD: .omc/state/sessions/dfc594af-64d6-4919-8f15-0aff2ed58294/prd.json
       (overwrote a stale leftover Phase 4 PRD from an earlier interrupted "start
       phase 4" invocation in this same session - Phase 4 was already committed/
       pushed before this run started)

ENVIRONMENT SETUP:
- Elasticsearch (investplus-es) and PostgreSQL (investplus-pg) containers were
  already running (docker ps confirmed, 4h uptime). finance_kb index confirmed
  populated: 1026 docs.
- Python deps: NOT installed in the base miniconda env, but ARE installed in
  the "main" conda env (conda activate main) - this is the env to use for this
  project, not base or "finreportrag".
- Backend run convention (from backend/AGENTS.md): `cd backend/app && uvicorn
  app_main:app --port 8000` (must run from backend/app, imports are flat, not
  package-qualified). Ran with PYTHONUNBUFFERED=1 for reliable log evidence.

DESIGN NOTE on "through the chat UI" (plan step 15):
- No browser-automation tool was available in this session. Verified via the
  actual production HTTP+SSE code path (POST /chat, the same endpoint the
  React frontend calls) instead of a literal browser click-through. The
  frontend does no transformation of the SSE payload that would change the
  tool-selection narrative - it renders exactly the agent/assistant role
  messages inspected here. Documented this as an explicit, defensible
  deviation in .omc/eval/phase5_e2e_verification.md rather than silently
  reinterpreting the acceptance criterion.

[done] US-1: Live stack verification. uvicorn started clean ("Application
startup complete"), /create_session returned 200, a trivial /chat round trip
streamed and terminated with event: end. Confirmed ES/PG connectivity via a
real rag_search-touching request in US-2 (a "hello" trivial query correctly
triggered NO tool calls per agent_plan's own rules, so had to use the US-2
run instead to prove ES connectivity - noted and corrected mid-run rather
than papering over it).

[done] US-2: AAPL complex query ("What is AAPL's current stock price, and
what recent news or 10-K risk factors might explain its recent
performance?") driven live. Result: 4 initial parallel actions
(finance_query x2, rag_search x2), then 5 full reflection iterations (hit
MAX_REFLECTION_ITERATIONS - a cap-stop, not an LLM-signaled stop, because
the query effectively asks for a single definitive cause of a 0.12% price
move, which doesn't exist as a clean answer - expected/correct behavior,
not a bug). Tools used across the run: finance_query, rag_search,
web_search (3 distinct). Every reflection-triggered action carried an
LLM-stated rationale string in the SSE content field (not a hardcoded
label). Final answer (2780 chars) cited real price data ($289.36, +0.12%),
real 10-K/10-Q risk-factor language, and real news items. Raw trace saved:
.omc/eval/phase5_traces/us2_aapl_complex_query.txt

[done] US-3: MSFT simple query ("What is MSFT's current stock price?")
driven live. Result: exactly 1 tool call (finance_query MSFT price),
should_continue() returned sufficient=true on iteration 1 (LLM-signaled
stop, not cap-stop), final answer correctly reported $373.02/-0.67%. Raw
trace saved: .omc/eval/phase5_traces/us3_msft_simple_query.txt

[done] US-4: Invalid ticker query ("What is ZZZZINVALID's current stock
price?") driven live. finance_tool's _is_valid_ticker guard returned a
graceful "未找到股票代码" string (NOT a raised exception - this is the
soft-error path in finance_tool.py, distinct from process_actions()'s
hard-exception "错误":true path from Phase 1). should_continue()'s
rationale explicitly recognized this as an invalid-ticker signal without
any hardcoded error flag - genuine LLM comprehension of unstructured error
text. HTTP 200 throughout, no traceback in server log, stream reached
event: end normally, final answer explicitly told the user the ticker was
invalid rather than fabricating data. Raw trace saved:
.omc/eval/phase5_traces/us4_invalid_ticker_query.txt

[done] US-5: Created backend/tests/test_e2e_finance.py (live smoke-test
script, same "not CI-automated" convention as test_agent.py, run via
`python backend/tests/test_e2e_finance.py` against a running backend) that
encodes the three scenarios' assertions programmatically. Ran it
independently (fresh sessions, fresh LLM calls, not reusing the manual
curl runs) - all 3 PASS. Caught and fixed an operator-precedence bug in my
own first draft of the AAPL test's assertion (a trailing `or "289" in
answer` clause would have short-circuited the whole check regardless of
HTTP status - fixed before relying on the script's output as evidence).
Wrote .omc/eval/phase5_e2e_verification.md documenting all three runs with
real captured data. Updated README.md: migration-status Phase 5 line
(⏳→✅... actually added fresh since it didn't exist before), lead
paragraph now says "verified live", Roadmap Phase 5 struck through
matching the ~~Phase 2~~/~~Phase 3~~/~~Phase 4~~ pattern, "Beyond Phase 4"
renumbered to "Beyond Phase 5".

INCIDENTAL FINDING (not fixed, out of Phase 5 scope): README.md links to
.omc/plans/finance-agent-migration-plan.md and .omc/eval/finance_eval_set.md,
but .omc/ is gitignored - these links are dead for anyone cloning from
GitHub. Predates Phase 5 (introduced during Phase 2), follows an existing
"all .omc/ content is local/session-scoped" convention. Flagged in the
findings doc and here for a future session/the user to decide whether to
fix (e.g. by moving these into a tracked docs/ location).

PHASE 5 COMPLETE (final phase of the 5-phase migration plan). All 5 PRD
stories pass with fresh live evidence, not assumed. Ready for architect
reviewer verification (Step 7).

[done] Architect reviewer verification (Step 7): APPROVED. Re-ran
backend/tests/test_e2e_finance.py itself with fresh live API calls (not
just reading saved traces) - all 3 scenarios passed independently. Audited
every line of the script's assertion logic (ok = (...) expressions) for
the operator-precedence bug class I'd already caught once - confirmed no
recurrence. Verified README markdown integrity (blank lines before
headings intact - the class of mistake from the Phase 4 review didn't
recur here either). Confirmed the .omc/ dead-link disclosure in the
findings doc is accurately scoped as pre-existing/out-of-scope.

[done] Deslop pass (Step 7.5): reviewed backend/tests/test_e2e_finance.py,
README.md, and .omc/eval/phase5_e2e_verification.md against the slop
checklist. No duplication, dead code, needless abstraction, or boundary
violations found. No edits made.

[done] Regression re-verification (Step 7.6): re-ran
backend/tests/test_e2e_finance.py one more time (yet another fresh
independent live run, since deslop made no changes) - all 3 scenarios
PASS, exit 0.

PHASE 5 COMPLETE. All 5 PRD stories pass, architect-approved with fresh
live re-execution (not rubber-stamped), deslop reviewed (no changes
needed), regression-verified twice independently. This was the final
phase of the 5-phase finance-agent migration plan
(.omc/plans/finance-agent-migration-plan.md) - Phases 1-5 are now all
complete. Stopped the uvicorn dev server that was started for this
session's live verification (not something the user asked to keep
running indefinitely).

---

RALPH RUN (2026-07-05): Fixed all 22 findings (6 Medium, 16 Low) from
debug/26_7_4_debug_3.md. Work split across 3 parallel executor agents by
domain:
- fixer-backend-api: M1, M2, L1-L4 (chat_rt.py, history_rt.py, agent.py,
  es_client.py, web_search.py, knowledgebase_operations.py)
- fixer-backend-rag: M3, M4, L5-L9 (pdf_parser.py, file_parse.py,
  index_finance.py, rag_tokenizer.py)
- fixer-frontend: M5, M6, L10-L16 (chat-message.tsx, markdown/index.tsx,
  App.tsx, sender/index.tsx, repository/index.tsx, chat/index.tsx,
  useSendMessage.ts, hot-questions/index.tsx, pages/index/index.tsx)

Architect verification (Step 7, THOROUGH tier/opus): APPROVED. Re-ran all
verification fresh (77 backend tests OK, frontend build success, lint at
exact 20-error/9-warning baseline), traced M1's exception handling, the
M3/M4 interaction inside the same pdf_parser.py method (no conflict
confirmed), L4's per-turn (not module-level) dedup set, and L8's doc_id
ordinal fix preserving re-upload idempotency.

Deslop pass (Step 7.5): reviewed all 27 changed files (incl. 4 new test
files). No duplication, dead code, or needless abstraction found -
comments are all non-obvious "why" explanations, deletions (serper_images/
videos, verify_user_knowledgebase/get_user_kb_files, dead rowSelection UI,
logging.basicConfig side effect) are clean. No edits made.

Regression re-verification (Step 7.6): re-ran full suite post-deslop
(no changes to re-verify against, but ran anyway per protocol) - 77/77
backend tests OK, frontend build succeeds, lint unchanged at 29/20/9.

Learnings:
- The 3-domain split (backend-api / backend-rag / frontend) from the
  review round carried over cleanly to the fix round with zero file
  overlap between workers - no merge conflicts.
- Two closely-related backend findings (L5 embedding-count-mismatch and
  L8 doc_id-hash-collision) both live in file_parse.py and were correctly
  assigned to the same worker (fixer-backend-rag) to avoid split
  ownership of one file.
- Pre-existing test-isolation issue found (not part of this round's 22
  fixes): `python -m unittest discover -s tests` fails on
  test_knowledgebase_operations.py because test_agent_loop.py's global
  sys.modules stub for "database.knowledgebase_operations" collides with
  it when run in the same discover process. Confirmed pre-existing
  (reproduces without this round's new test files too). All this round's
  verification runs used explicit module names, not discover, so it
  didn't affect results. Worth fixing in a future round if discover-mode
  test running is ever needed.
- Checkmarks were added by the team lead centrally to debug_3.md and
  debug_3_zh.md (not by the fixer agents) to avoid 3-way concurrent-edit
  conflicts on the same two report files.

Chinese fix report: debug/26_7_4_debug_3_fix_report_zh.md.
RALPH RUN COMPLETE.

=== US-002 backend router/data layer fixes (fixer-2) ===
H3 (info leak): replaced raw str(e)/f"...{e}" in HTTPException(detail=...) with generic "内部错误，请稍后重试" + logger.exception at all 6 sites:
  - chat_rt.py:65 create_session (added logger.exception)
  - chat_rt.py get_files except (was f"get_files failed: {e}")
  - chat_rt.py get_official_examples except
  - chat_rt.py delete_file except
  - history_rt.py delete_session except (added logging import + logger)
  - history_rt.py get_sessions except
  - history_rt.py get_messages except
M1 (indexes): init.sql — added composite CREATE INDEX idx_messages_session_id_created_at ON messages(session_id, created_at); sessions(user_id) index already existed (idx_sessions_user_id). Also added Index(session_id, created_at) __table_args__ and user_id index=True to models/message.py so create_all stays in sync. Schema source is init.sql (init_db/create_all not called in main.py).
M2 (scoping): history_rt.py delete_session + get_messages now scope by user via EXISTS(SELECT 1 FROM sessions WHERE sessions.session_id = messages.session_id AND sessions.user_id = :uid) — messages table has no user_id column, so equivalent scoping via sessions ownership.
M3 (ES leak): es_client.py get_es_client() — now a module-level double-checked-lock singleton (threading.Lock), mirroring agent.py _get_es().
M4 (pool): database.py create_engine now has pool_pre_ping=True, pool_recycle=1800.
L6 (pagination): chat_rt.py get_files() — composite agg now loops following after_key until no more buckets.
L7 (status): chat_rt.py upload_files() — top-level status computed from per-file results: success/partial/failed.
L8 (config): database.py — raises RuntimeError if DATABASE_URL unset, before create_engine.
L9 (SSE regex): chat_rt.py:160 — re.search now uses re.DOTALL so multi-line data payloads aren't truncated.

Tests (run individually):
  test_chat_router.py    -> 17 passed
  test_history_router.py ->  6 passed
  test_es_client.py      ->  5 passed
No regressions introduced.

=== Frontend findings (US-003) — fixer-3 ===
H4  chat/index.tsx: added AbortController per sendChat (abortRef), pass signal to api.session.chat, store reader in readerRef, useEffect unmount cleanup aborts+cancels; aborts caught silently via controller.signal.aborted. (index.tsx:9,105-116,140-165,255-262)
M9  chat/index.tsx:255-262: useMount send() now .catch(() => {}) to avoid unhandled rejection on first-message failure.
M10 components/markdown/index.tsx:15-63: throttled parse/sanitize to ~100ms (leading+trailing) via committed state; raw value updated immediately, trailing timer guarantees final render. Removes O(n^2) per-token full re-parse jank.
M11 pages/repository/index.tsx:146-166: delete button wrapped in antd Popconfirm showing file_name, danger OK; new i18n keys repoDeleteConfirmTitle/Ok/Cancel (i18n.tsx zh+en).
L10 components/markdown/index.tsx:8-13: DOMPurify.addHook afterSanitizeAttributes forces rel="noopener noreferrer" on target=_blank anchors.
L11 api/request/index.ts:3-6: added comment noting CSRF layer should be revisited if backend moves to cookie-based auth (no scheme implemented).
L12 i18n.tsx:126-165: language persisted to localStorage; init reads localStorage -> navigator.language -> 'en'; currentLang module var seeded the same way.
L13 pages/chat/component/result.tsx:296-298: comment documenting like/dislike is intentionally local-only, no backend endpoint.
L14 utils/usePageTransport.ts:2,13-27: ref-based consumed guard instead of unconditional delete, so StrictMode double-invoke doesn't drop/duplicate first message.
L15 pages/repository/components/upload.tsx:99 accept=".txt,.md,.pdf" on Dragger; pages/repository/index.tsx:233-234 okButtonProps={{loading:uploading}} + cancelButtonProps disabled while uploading.
L16 pages/repository/index.tsx:98-101: dayjs.utc(value).isValid() check, falls back to '-' on invalid updated_at.
Build: PASSED (tsc -b && vite build, built in ~4.3s, no errors).
Lint: no NEW errors introduced (baseline 8 pre-existing errors in the two touched files with pre-existing issues; current 7 — all on untouched pre-existing lines).

=== US-001 backend agent/service layer fixes (fixer-1, 2026-07-05) ===
Files: backend/app/service/agent/agent.py, backend/app/service/finance/finance_tool.py

H1 (request storm): _adjust_format() now coerces `prompts` — str→[str], non-str/non-list→[] —
  before iterating (agent.py:_adjust_format). Prevents char-by-char iteration of a string
  `prompts` value spawning one real external API call per character. Same path serves
  should_continue()'s reflect actions (both go through _adjust_format), so it's covered too.

H2 (LLM calls unguarded): VERIFIED already mitigated at the router level. chat_rt.py stream()
  (lines 154-187) wraps `for event in final_answer(...)` in try/except that catches ANY
  exception from the generator (agent_plan/should_continue run synchronously inside
  final_answer before the relevant yields), logs it, yields a friendly bilingual error +
  `event: end / data: [DONE]`, and _persist(stream_failed=True) skips saving an empty answer.
  So the SSE stream is NOT left hanging. Defensive hardening added on top for graceful
  per-stage degradation instead of whole-response abort:
    - agent_plan(): _llm_json call wrapped in try/except → transient planning error degrades
      to "no tool calls" (returns None → direct answer from model knowledge).
    - should_continue(): _llm_json call wrapped in try/except → transient reflect error falls
      back to {sufficient:True, actions:[]} (stop reflecting, answer with what's gathered);
      rationale tagged "[llm error - defaulting to stop]" to distinguish from LLM-signaled stop.
    - Final streaming create() left as-is: once mid-stream there's no graceful degradation
      possible, so it correctly aborts into the router's existing handler.

M5 (yfinance timeout): yfinance installed is 1.5.1 (uses curl_cffi 0.15.0). A plain
  requests.Session IS accepted (is_supported_session→True) and routes yfinance to the plain
  requests backend, so the session-level timeout does apply to no-explicit-timeout requests.
  (Note: functools.partial kwargs are OVERRIDDEN by call-time kwargs, so there is NO
  timeout-collision TypeError even though data.py passes its own timeout on cookie/crumb calls.)
  Could not conclusively verify the socket timeout covers every hang path (rate-limited before
  observation; curl-fallback/DNS/connect edge cases), so per instruction added a ThreadPoolExecutor
  wall-clock ceiling (_YF_WALL_CLOCK_SECONDS=30) around _load_ticker's calls; on timeout it raises
  concurrent.futures.TimeoutError (caught by each query_* → friendly message), and shuts the
  executor down with wait=False so a hung thread never blocks the caller. Verified with a mocked
  hanging Ticker: _load_ticker returns bounded, query_quote degrades gracefully.

M6 (json_object vs list inconsistency): agent_plan's prompt now asks for a top-level JSON object
  {"actions":[...]} / {"actions": null} (consistent with _llm_json's forced
  response_format=json_object). Parsing switched from _extract_json_list to _extract_json_object +
  parsed.get("actions"); returns the same shape callers expect (list of action dicts, or None).
  Removed the now-unused _extract_json_list helper. (agent.py:agent_plan)

M7 (KeyError on history): history loop now uses turn.get('user','') / turn.get('assistant','')
  instead of direct indexing. (agent.py, final_answer history_str build)

M8 (rag dedup vs volatile _score): added _dedup_result_key() — builds the dedup key from stable
  fields only, excluding _score and any float field; process_actions() uses
  (prompt, _dedup_result_key(result)). Near-identical re-retrievals now dedup despite rerank
  score drift. (agent.py:process_actions / _dedup_result_key)

L1 (query_news no guard): added try/except around _load_ticker in query_news, returning the same
  Chinese "行情服务暂时不可用，请稍后再试。" fallback as query_quote/query_fundamentals.
  (finance_tool.py:query_news)

L2 (dividend_yield boundary): yfinance 1.5.x passes Yahoo's raw dividendYield through with no
  normalization → unit not reliably inferable, so logic left unchanged per instruction; expanded
  the comment documenting the known sub-1% percent-format misfire edge case. (finance_tool.py)

L3 (wrong annotation): web_search return type corrected from -> list[str] to -> list[dict].

L4 (single-ticker drop): added a comment documenting the intentional single-ticker-per-subquestion
  convention (Plan stage splits compound questions), no behavior change. (agent.py:finance_tool)

L5 (fragile validity heuristic): _is_valid_ticker now requires a meaningful field
  (info.get("symbol") or info.get("regularMarketPrice")) alongside len(info)>2 when history is
  empty, in addition to the non-empty-history fast path. (finance_tool.py:_is_valid_ticker)

Test command + output:
  cd backend && PYTHONPATH=app <py> -m pytest tests/test_agent_loop.py tests/test_finance_tool.py -q
  Result: 1 failed, 68 passed, 11 subtests passed.
  The single failure (BoundedMemoryTests::test_final_answer_prompt_uses_bounded_memory_not_raw)
  is PRE-EXISTING and NOT caused by these changes: it fails byte-identically on the original code
  (verified via git stash of both files → same 1 failed / 68 passed), passes in isolation, and is a
  test-isolation artifact of the module-level _llm_client singleton (an earlier test populates it,
  so this test's patch of agent.OpenAI no-ops). No new failures introduced.
